% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_ofe_grid.R
\name{all_cells_grid}
\alias{all_cells_grid}
\title{Build OFE grid and (initially) select all cells}
\usage{
all_cells_grid(data, cellsize, angle_deg = 0, buffer = 0, shift = c(0, 0))
}
\arguments{
\item{data}{An `sf` object of points with geometry.}

\item{cellsize}{numeric length-1 or length-2, grid cell size.}

\item{angle_deg}{numeric, grid rotation in degrees.}

\item{buffer}{numeric, buffer added around data bbox before gridding (same units as CRS).}

\item{shift}{numeric length-2, offset added to grid origin (xmin, ymin).}
}
\value{
An object of class `ofe_grid` (a list) with:
  \itemize{
    \item `grid_all`: `sf` polygons of the full grid (with `CellID`).
    \item `grid_sel`: initially identical to `grid_all` (no filtering yet).
    \item `params`: a named list of grid parameters (cellsize, shift, angle, buffer, CRS).
  }
}
\description{
Build OFE grid and (initially) select all cells
}
